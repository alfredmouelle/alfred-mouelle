import { ArrowUp, Home, Moon, Settings, Settings2, SunMedium, User2 } from 'lucide-react';

export const Icons = {
  sunMedium: SunMedium,
  moon: Moon,
  home: Home,
  user: User2,
  settings: Settings2,
  arrowUp: ArrowUp
}